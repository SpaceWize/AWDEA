# Dependencies
node_modules/
yarn.lock

# Build outputs — CI builds and deploys these, so they are not committed.
dist/
docs/
build/
.env.local
.env.*.local
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Claude Code
.claude/

# Alternate design, kept local-only until it's ready for its own repo.
newsite/
